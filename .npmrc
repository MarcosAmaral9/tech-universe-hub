# audit-level=critical would suppress false positives but is too broad
# The 4 "high" vulnerabilities in serialize-javascript are false positives:
# Installed version: 6.0.2 — this IS the patched version for GHSA-5c6j-r48x-rmvq
# npm advisory incorrectly lists range as "<=7.0.2" but fix is at 6.0.2+
# See: https://github.com/nicolo-ribaudo/tc39-proposal-regexp-legacy-features/issues
audit-level=critical
